def count_vowels(s):
    """
    Returns: number of vowels in a string

    Vowels are a,e,i,o,u

    Upper case and lower case both work

    Examples:...

    Paramter s: The string to check for vowels
    Precondition: s is a string
    """
    pass

s="0123"
print(s[1:-1])
#second argument (denoting end of slice index) only goes up to the index of that number -1, both strings and lists
#no error from the second argument if its larger than the index
# l=[0,1,2,3]
# print(l[:2])
# escape="\\hello\'"
# print(escape)
# print(len(escape))
